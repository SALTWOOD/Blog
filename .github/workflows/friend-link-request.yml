name: Friend Link Request

on:
  issues:
    types: [opened]
  issue_comment:
    types: [created]

jobs:
  process:
    if: >
      (github.event_name == 'issues' || github.event_name == 'issue_comment') &&
      contains(github.event.issue.body, '### 博客名称') &&
      (
        github.event_name == 'issues' ||
        (github.event_name == 'issue_comment' && github.event.comment.body == '/review' && github.event.issue.state == 'open')
      )
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      - uses: actions/checkout@v4

      - name: Install WebP tools
        run: |
          if ! command -v cwebp >/dev/null 2>&1; then
            sudo apt-get update && sudo apt-get install -y webp
          fi

      - name: Parse, validate, and process
        uses: actions/github-script@v7
        with:
          script: |
            const body = context.payload.issue.body.replace(/\r\n/g, "\n");

            function extractField(title) {
              const regex = new RegExp(`### ${title}\\s*\\n+([\\s\\S]*?)(?=\\n###|$)`, "i");
              const match = body.match(regex);
              return match?.[1]?.trim() ?? "";
            }

            const fs = require("fs");
            const path = require("path");
            const os = require("os");
            const { execSync } = require("child_process");

            const SUPPORTED_FORMATS = ["webp", "png", "jpeg", "gif", "bmp"];

            // Sniff the real format from magic bytes (more reliable than Content-Type).
            function detectImageFormat(buf) {
              if (!buf || buf.length < 12) return null;
              const ascii = (start, len) => buf.subarray(start, start + len).toString("ascii");
              if (ascii(0, 4) === "RIFF" && ascii(8, 4) === "WEBP") return "webp";
              if (buf[0] === 0x89 && ascii(1, 3) === "PNG") return "png";
              if (buf[0] === 0xff && buf[1] === 0xd8 && buf[2] === 0xff) return "jpeg";
              const gifSig = ascii(0, 6);
              if (gifSig === "GIF89a" || gifSig === "GIF87a") return "gif";
              if (buf[0] === 0x42 && buf[1] === 0x4d) return "bmp"; // "BM"
              return null;
            }

            // Download the icon and return it as a WebP buffer, converting if needed.
            async function prepareWebpAvatar(url) {
              let resp;
              try {
                resp = await fetch(url);
              } catch (e) {
                return { error: "无法访问图标 URL: " + e.message };
              }
              if (!resp.ok) return { error: "图标 URL 返回了非成功状态码: " + resp.status };

              const buf = Buffer.from(await resp.arrayBuffer());
              const format = detectImageFormat(buf);
              if (!format) {
                return { error: "图标格式不受支持，支持的格式: " + SUPPORTED_FORMATS.join(" / ").toUpperCase() };
              }
              if (format === "webp") return { buffer: buf };

              const tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), "avatar-"));
              const inPath = path.join(tmpDir, "input." + format);
              const outPath = path.join(tmpDir, "output.webp");
              fs.writeFileSync(inPath, buf);
              try {
                execSync(`cwebp -quiet -q 90 "${inPath}" -o "${outPath}"`, { stdio: "pipe" });
                return { buffer: fs.readFileSync(outPath) };
              } catch (e) {
                const detail = e.stderr ? e.stderr.toString().trim() : e.message;
                return { error: "图标转换为 WebP 失败: " + detail };
              }
            }

            const name = extractField("博客名称");
            const url = extractField("网址");
            const description = extractField("简介");
            const slug = extractField("标识");
            const iconUrl = extractField("图标 URL");

            const errors = [];

            if (!name) errors.push("博客名称 不能为空");
            if (!description) errors.push("简介 不能为空");

            if (!url) {
              errors.push("网址 不能为空");
            } else {
              try { new URL(url); } catch { errors.push("网址格式不合法: " + url); }
            }

            if (!slug) {
              errors.push("标识 不能为空");
            } else if (!/^[a-zA-Z0-9]([a-zA-Z0-9-]*[a-zA-Z0-9])?$/.test(slug)) {
              errors.push("标识 只能包含英文字母、数字和连字符，且不能以连字符开头或结尾");
            }

            if (iconUrl) {
              try { new URL(iconUrl); } catch { errors.push("图标 URL 格式不合法: " + iconUrl); }
            }

            let avatarBuffer = null;
            if (errors.length === 0) {
              const result = await prepareWebpAvatar(iconUrl);
              if (result.error) errors.push(result.error);
              else avatarBuffer = result.buffer;
            }

            const linksPath = path.join(process.env.GITHUB_WORKSPACE, "src", "data", "links.json");

            let linksData = [];
            if (fs.existsSync(linksPath)) {
              try {
                const raw = JSON.parse(fs.readFileSync(linksPath, "utf-8"));
                if (Array.isArray(raw)) linksData = raw;
              } catch {}
            }

            for (const entry of linksData) {
              if (entry.slug === slug) errors.push("标识 '" + slug + "' 已存在，请换一个");
              if (entry.url === url) errors.push("该网址已在友链中，请勿重复申请");
            }

            if (errors.length > 0) {
              const comment = [
                "## 校验未通过",
                "",
                ...errors.map(e => "- " + e),
                "",
                "请修正上面的问题后，直接评论 `/review` 以触发自动检查或者等待维护者审查。",
              ].join("\n");
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: comment,
              });
              return;
            }

            const avatarPath = path.join(process.env.GITHUB_WORKSPACE, "src", "assets", "friends", slug + ".webp");
            fs.mkdirSync(path.dirname(avatarPath), { recursive: true });
            fs.writeFileSync(avatarPath, avatarBuffer);

            linksData.push({ slug, name, url, description });
            fs.writeFileSync(linksPath, JSON.stringify(linksData, null, 2) + "\n", "utf-8");

            const branchName = "friend-link/" + slug + "-" + Date.now();
            execSync("git config user.name 'github-actions[bot]'");
            execSync("git config user.email 'github-actions[bot]@users.noreply.github.com'");
            execSync("git checkout -b " + branchName);
            execSync('git add "src/data/links.json" "src/assets/friends/' + slug + '.webp"');
            const opener = context.payload.issue.user;
            const coAuthorTrailer = "Co-Authored-By: " + opener.login + " <" + opener.id + "+" + opener.login + "@users.noreply.github.com>";
            execSync('git commit -m "chore(links): ' + name.replace(/"/g, '\\"') + '" -m "' + coAuthorTrailer + '"');
            execSync("git push origin " + branchName);

            const prBody = [
              "来自 #" + context.issue.number + " 的友链申请",
              "This PR closes #" + context.issue.number + ".",
              "",
              "| 字段 | 内容 |",
              "|------|------|",
              "| 博客名称 | " + name + " |",
              "| 网址 | " + url + " |",
              "| 简介 | " + description + " |",
              "| 标识 | " + slug + " |",
            ];

            await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: "chore(links): " + name,
              head: branchName,
              base: "main",
              body: prBody.join("\n"),
            });
